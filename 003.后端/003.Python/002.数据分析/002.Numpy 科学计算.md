---
date: 2025-11-17 11:02
modified: 2025-11-17 11:49
---

# 介绍

Numpy 是 Python 中科学计算的基础包。它是一个 Python 库，提供多维数组对象、各种派生对象（例如掩码数组和矩阵）以及用于对数组进行快速操作的各种方法，包括数学、逻辑、形状操作、排序、选择、I/O 、离散傅里叶变换、基本线性代数、基本统计运算、随机模拟等

Numpy 的部分功能：

- ndarray，一个具有矢量算术运算和复杂广播能力的快速且节省空间的多维数组
- 用于对整组数据进行快速运算的标准数学函数（无需编写循环）
- 用于读写磁盘数据的工具以及用于操作内存映射文件的工具
- 线性代数、随机数生成以及傅里叶变换功能
- 用于集成由 C、C++、Fortran 等语言编写的代码的 API

为什么需要 Numpy？

```python
# 原生 Python 列表 VS. NumPy 数组性能对比
import time
import numpy as np
  
py_list = list(range(1000000))  
np_arr = np.arange(1000000)  
  
# 计算平方和  
start = time.time()  
sum([x**2 for x in py_list])  
print(f"Python 列表耗时: {time.time()-start:.4f}s")
# Python 列表耗时: 0.1256s    
  
start = time.time()  
np.sum(np_arr**2)  
print(f"NumPy 数组耗时: {time.time()-start:.4f}s")
# NumPy 数组耗时: 0.0023s
```

# ndarray

## 核心特性

- **多维性**：支持 0 维（标量）、1 维（向量）、2 维（矩阵）及更高维（张量）数组
- **同质性**：所有元素类型必须一致（通过 `dtype` 指定）
- **高效性**：基于连续内存块存储，支持向量化运算

## ndarray 的属性

例如 `arr = np.array([[1, 2], [3, 4]])`：

**核心属性（必须掌握）**

| 属性名称    | 通俗解释                       | 输出结果               |
| ------- | -------------------------- | ------------------ |
| `shape` | **数组的形状**：行数和列数（或更高维度的尺寸）  | `(2, 2)`           |
| `ndim`  | **维度数量**：数组是几维的            | `2`                |
| `size`  | **总元素个数**：数组中所有元素的总数       | `4`                |
| `dtype` | **元素类型**：数组中元素的类型（整数、浮点数等） | `int64`（或 `int32`） |

**进阶属性（了解即可）**

| 属性名称       | 通俗解释                           | 输出结果                    |
| ---------- | ------------------------------ | ----------------------- |
| `T`        | **转置**：行变列，列变行                 | `[[1, 3], [2, 4]]`      |
| `itemsize` | **单个元素占用的内存字节数**               | `8`（`int64` 类型占 8 字节）   |
| `nbytes`   | **数组总内存占用量**：`size * itemsize` | `32`（4 元素 × 8 字节）       |
| `flags`    | **内存存储方式**：是否连续存储（高级优化）        | `C_CONTIGUOUS : True` 等 |
